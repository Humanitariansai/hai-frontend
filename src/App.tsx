import React from 'react';
import { BrowserRouter as Router, Route, Routes } from 'react-router-dom';

const App = () => {
  return (
    <Router>
      <div>
        <h1>Humans + AI {'>'} AI</h1>
      </div>
    </Router>
  );
};

export default App;